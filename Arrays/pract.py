arr = [1, 2, 7, 4, 5]
for i in range(len(arr)): # the i is the index of the array, it will go from 0 to 4 in this case.
    print(i, arr[i]) # the first i is the index and the second i is the value at that index. How the 2 i with 2 values?
    # The first i is the index of the array, it will go from 0 to 4 in this case. The second i is the value at that index. So when i is 0, arr[i] is 1, when i is 1, arr[i] is 2, and so on. 